{
  "name": "html-css",
  "version": "1.0.0",
  "description": "HTML-CSS course",
  "main": "index.js",
  "scripts": {
    "copy": "rm -rf dist && mkdir dist && cp _redirects dist && cp -R src/data dist && cp -R src/img dist",
    "start": "npm run copy && parcel src/index.html",
    "build": "npm run copy && parcel build src/index.html"
  },
  "author": "bcalou",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.13.1",
    "parcel": "^1.9.7",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "reset-css": "^4.0.1"
  }
}
